import type {
	Store,
	State,
	Getter,
	Action,
	GetterMap,
	ActionMap,
	StoreOption
} from './types'

import { createCup } from './cup'

import type { Pool } from './pool'

export { createCup }

export type {
	Store,
	State,
	Getter,
	Action,
	GetterMap,
	ActionMap,
	StoreOption,
	Pool
}
